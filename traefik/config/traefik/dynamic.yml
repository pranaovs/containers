http:
  routers:
    http-catchall:
      rule: "HostRegexp(`^.+$`)"
      entryPoints:
        - web
      middlewares:
        - https-redirect
      service: noop@internal
      priority: 1

  middlewares:
    https-redirect:
      redirectScheme:
        scheme: https
        permanent: true
